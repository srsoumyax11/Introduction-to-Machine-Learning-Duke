1. Which one of the following models, when used for image classification, can exceed the performance of humans?
- Multilayer perceptron
- Logistic regression
- ```Convolutional neural network```

2. Which of the following best describes the hierarchical structure of images, listed from most complex to simplest?
- Sub-motifs, high-level motifs, and atomic elements
- Atomic elements, sub-motifs, and high-level motifs
- High-level motifs, atomic elements, and sub-motifs
- ```High-level motifs, sub-motifs, and atomic elements```

3. In the CNN explained in this lesson, which of the following allows a classification decision to be made?
- Layer 2 feature map
- ```Layer 3 feature map```
- High-level motifs
- Sub-motifs